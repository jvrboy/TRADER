<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:TraderUI.ViewModels"
             x:Class="TraderUI.Views.QuoteDetailPage"
             x:DataType="vm:QuoteDetailViewModel"
             Title="{Binding Quote.Symbol}">

    <Grid RowDefinitions="Auto,*">
        <!-- Header -->
        <Frame Grid.Row="0" Style="{StaticResource Card}" CornerRadius="0" Margin="0">
            <StackLayout Spacing="8" IsVisible="{Binding Quote, Converter={StaticResource NotNullConverter}}">
                <Grid ColumnDefinitions="*,Auto">
                    <StackLayout Grid.Column="0">
                        <Label Text="{Binding Quote.Symbol}" Style="{StaticResource HeadlineMedium}"/>
                        <Label Text="{Binding Quote.Name}" Style="{StaticResource BodyMedium}"/>
                    </StackLayout>
                    <StackLayout Grid.Column="1" HorizontalOptions="End">
                        <Label Text="{Binding Quote.PriceDisplay}" Style="{StaticResource HeadlineMedium}" HorizontalOptions="End"/>
                        <Label Text="{Binding Quote.ChangePercentDisplay}"
                               TextColor="{Binding Quote.IsPositive, Converter={StaticResource BoolToColorConverter}}"
                               FontSize="16" FontAttributes="Bold" HorizontalOptions="End"/>
                    </StackLayout>
                </Grid>
                <Grid ColumnDefinitions="*,*,*">
                    <StackLayout Grid.Column="0">
                        <Label Text="HIGH" Style="{StaticResource BodySmall}"/>
                        <Label Text="{Binding Quote.High, StringFormat='{0:F5}'}" FontSize="13" TextColor="{StaticResource Success}"/>
                    </StackLayout>
                    <StackLayout Grid.Column="1">
                        <Label Text="LOW" Style="{StaticResource BodySmall}"/>
                        <Label Text="{Binding Quote.Low, StringFormat='{0:F5}'}" FontSize="13" TextColor="{StaticResource Danger}"/>
                    </StackLayout>
                    <StackLayout Grid.Column="2">
                        <Label Text="VOLUME" Style="{StaticResource BodySmall}"/>
                        <Label Text="{Binding Quote.Volume, StringFormat='{0:N0}'}" FontSize="13" TextColor="{StaticResource TextPrimary}"/>
                    </StackLayout>
                </Grid>
            </StackLayout>
        </Frame>

        <!-- Chart + Analysis -->
        <ScrollView Grid.Row="1">
            <StackLayout Padding="16" Spacing="12">
                <!-- AI Analysis -->
                <Frame Style="{StaticResource Card}" IsVisible="{Binding Analysis, Converter={StaticResource NotNullConverter}}">
                    <StackLayout>
                        <Label Text="AI Swarm Analysis" Style="{StaticResource TitleMedium}" Margin="0,0,0,8"/>
                        <Grid ColumnDefinitions="*,Auto,Auto">
                            <Label Grid.Column="0" Text="{Binding Analysis.OverallBias}"
                                   TextColor="{StaticResource Primary}" FontAttributes="Bold" FontSize="16"/>
                            <Label Grid.Column="1" Text="{Binding Analysis.BullishScore, StringFormat='↑{0:F0}%'}"
                                   TextColor="{StaticResource Success}" FontSize="14" Margin="0,0,8,0"/>
                            <Label Grid.Column="2" Text="{Binding Analysis.BearishScore, StringFormat='↓{0:F0}%'}"
                                   TextColor="{StaticResource Danger}" FontSize="14"/>
                        </Grid>
                        <Label Text="{Binding Analysis.Summary}" Style="{StaticResource BodyMedium}" Margin="0,8,0,0"/>
                    </StackLayout>
                </Frame>

                <!-- Loading -->
                <ActivityIndicator IsVisible="{Binding IsBusy}" IsRunning="{Binding IsBusy}"
                                   Color="{StaticResource Primary}" HorizontalOptions="Center"/>
            </StackLayout>
        </ScrollView>
    </Grid>
</ContentPage>
